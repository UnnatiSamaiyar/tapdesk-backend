 
    <div class="container">
        <div class="header">
            <h1>Account Created Successfully</h1>
        </div>
        <div class="content">
            <p>Dear {{name}},</p>
            <p>We are pleased to inform you that your employee account has been created. Below are your account details:</p>
            <table class="details-table">
                <tr>
                    <th>Name:</th>
                    <td>{{name}}</td>
                </tr>
                <tr>
                    <th>Email:</th>
                    <td>{{email}}</td>
                </tr>
                <tr>
                    <th>Phone:</th>
                    <td>{{phone}}</td>
                </tr>
                <tr>
                    <th>Role:</th>
                    <td>{{role}}</td>
                </tr>
                <tr>
                    <th>Product :</th>
                    <td>{{productCategory}}</td>
                </tr>
                <tr>
                    <th>Created At:</th>
                    <td>{{createdAt}}</td>
                </tr>
                <tr>
                    <th>Created By:</th>
                    <td>{{createdByName}}</td>
                </tr>
            </table>
            <p class="note">If you haven't received the password from the admin, please use the <a href="{{forgotPasswordLink}}" class="btn">Forgot Password</a> option to create a new one.</p>
        </div>
         
    </div>
